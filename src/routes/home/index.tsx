import React, { Component } from 'react';

import {
     Header,
     Footer
} from '../../components';

export default class Home extends Component {
    render() {
        return (
            <>
                <Header />
                <Footer />
            </>
        );
    }
}